@using FA.JustBlog.ViewModel.ViewModel;
@using System.Security.Claims
@using FA.JustBlog.Core.Utill
@model IList<CategoryViewModel>

@addTagHelper *,Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1>All Categories</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th> 
            <th>
                Description
            </th>
            @if (User.Claims.Any(c => c.Type == ClaimTypes.Role && (c.Value == $"{RoleUnit.Role_BlogOwner}")))
            {
                <th>Function</th>
            }
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    <a href="/Admin/Category/Details?id=@item.Id">@Html.DisplayFor(modelItem => item.Name)</a>
            </td> 
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
               
                   
                    
                    @if (User.Claims.Any(c => c.Type == ClaimTypes.Role && (c.Value == $"{RoleUnit.Role_BlogOwner}")))
                {
                    <td>
                        <a href="/Admin/Category/Edit?id=@item.Id">Edit</a>
                        <a href="/Admin/Category/Delete?id=@item.Id">Delete</a>
                    </td>
                    }
            
        </tr>
}
    </tbody>
</table>
